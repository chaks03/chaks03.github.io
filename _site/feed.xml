<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2026-04-08T12:19:14+00:00</updated><id>/feed.xml</id><title type="html">Chaks | SRE &amp;amp; Quant Systems</title><subtitle>Your Name&apos;s academic portfolio</subtitle><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><entry><title type="html">Using post-mortem report as a medium to build trust across cross-team members</title><link href="/posts/2020/10/postmortem-trust/" rel="alternate" type="text/html" title="Using post-mortem report as a medium to build trust across cross-team members" /><published>2020-10-17T00:00:00+00:00</published><updated>2020-10-17T00:00:00+00:00</updated><id>/posts/2020/10/postmortem-trust</id><content type="html" xml:base="/posts/2020/10/postmortem-trust/"><![CDATA[<p>We know that “post-mortem” is a term that is widely used in various criminal investigations. By analyzing the various subjects of crime and mentioning them in the “Post-mortem report” helps the mystery of crime to be revealed by conducting a thorough investigation on them. Today we will discuss how this “post-mortem report” plays an important role in modern software development as a medium of better communication across cross-team members.</p>

<p>Let’s think of a simple scenario without going to a high level. Sometimes it may happen that when a new software module is released, it is seen that the software service is not as stable as before. The application or service begins to behave inconsistently. But yes, one thing we must keep in mind is that any inadequately tested software code, incorrect configuration of application runtime and dependencies on the server, or inadequate server resource (CPU, RAM, storage) design can also cause the software service to fall into such situation. It may also happen that the software system is relying upon third party services. And when the third party service is down, the software service stability becomes uncertain. If this circumstance keeps repeating again and again, then the number of software users will continue to decrease to an extent, which will eventually increase the business losses. At one stage, of course, the software development, quality, testing, and system operations team must come to terms with top management. And if there is no proper documentation and communication among the team, then there is a possibility of huge disagreement which is inevitably very terrible. One solution to this crisis is the “post-mortem” report.</p>

<p><strong>“In the midst of chaos, there is also opportunity” - Sun-Tzu, The Art of War Illustrated</strong></p>

<p>A postmortem report is a template that contains a detailed description of software misbehavior or software service downtime. The parts of the whole report are - Title, date, downtime status, summary, impact, detection, evidence, root cause, resolution, mitigating the problem in the future, and contributors. However, some parts may be optional. The early part of this report is presented in a very general way so that anyone from the non-technical team can understand the report at first glance. Another benefit of writing this report is that it makes you aware of the limitations of the team involved in the whole incident. Limitations may include - Team member crisis, lack of training, miscommunication, lack of good service monitoring system, etc. And if these limitations are clearly manifested, the top management has to be ready to come forward to solve them.</p>

<p>You might think that this “post-mortem report” is overkill. Its not actually. Because with a “post-mortem report” you can create a “blameless” culture. This will increase the trust among the team members towards each other. And “trust” is always blended in a successful teamwork.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="communication" /><category term="devops" /><category term="sre" /><category term="software" /><category term="postmortem" /><summary type="html"><![CDATA[We know that “post-mortem” is a term that is widely used in various criminal investigations. By analyzing the various subjects of crime and mentioning them in the “Post-mortem report” helps the mystery of crime to be revealed by conducting a thorough investigation on them. Today we will discuss how this “post-mortem report” plays an important role in modern software development as a medium of better communication across cross-team members.]]></summary></entry><entry><title type="html">Some theories that I find practical in software engineering</title><link href="/posts/2020/09/hypothesis-se/" rel="alternate" type="text/html" title="Some theories that I find practical in software engineering" /><published>2020-09-26T00:00:00+00:00</published><updated>2020-09-26T00:00:00+00:00</updated><id>/posts/2020/09/hypothesis-se</id><content type="html" xml:base="/posts/2020/09/hypothesis-se/"><![CDATA[<p>Today I will highlight a few theories that are well related to some of the extents of software engineering. “Software engineering” is a method that divides the whole development process into smaller modules and moves towards the solution through detailed analysis. Each of these small modules is the sum of one or more problems.</p>

<p>And one of the special things about “software engineering” is that here you have to think and study very well about both “problems” and possible “solutions”. Because each solution will somehow create new problems, albeit in a very subtle way. Let’s highlight the facts without exaggerating this time. Each theory will be mentioned with a link reference at the bottom of the post.</p>

<ul>
  <li>“Something that can go wrong will go wrong” [1]</li>
  <li>“Simple things should be simple, complex things should be possible” [2]</li>
  <li>Cobra Effect [3]</li>
</ul>

<p>The first theory that emerges for us is that any object that can behave erratically because it is not given enough attention will behave erratically, even at the most necessary time. Here are some examples - not keeping backups on time, not listing application bugs, not giving priority to fixing security loopholes, etc.</p>

<p>The second theory is actually much broader and does not need much detailed explanation here. In real life we all want complex goals to be achieved. The same is true when designing a software module but try to figure out how easy it can be. However, applying it in many cases becomes much more challenging. Even if a software technology/architecture is made simple you cannot always bet on the reliability. Things will anyhow get complicated.</p>

<p>The third theory seems quite interesting to me. I found this term when I was reading an article about marketing. The story behind this theory is that there were many cobra infestations in Delhi at the beginning of British rule. So the then government announced that instead of bringing a dead cobra, it would be rewarded. A few days later it was really noticed that the infestation of snakes had decreased considerably. As time went on, it became clear that the frequency of rewards increased again, which was unusual. After searching, it was found that some unscrupulous people used to breed and kill the cobras through cobra farming and take the rewards in return. The government then banned the reward program. Since there was no way to make a profit, the cobra farms were opened resulting in the presence of more cobra snakes than in the initial stage.</p>

<p>The essence of this theory is that when any method we apply to solve a problem exacerbates that problem after a certain period of time, it is called the “cobra effect”. For example, if you fix a complex application bug without proper testing, and sooner or later if it starts leaking memory, will cause the application to crash. The purpose of the bug fix was to make the application better but it temporarily fixed the problem but in the long run, it made the problem even worse.</p>

<p>In addition to the above three theories, there are many other theories that are discussed in various fields of “software engineering”. However, today’s discussion will be limited to this.</p>

<ul>
  <li>[1] “Anything that can go wrong will go wrong” - <a href="https://bit.ly/36575sR">Murphy’s law</a></li>
  <li>[2] “Simple things should be simple, complex things should be possible” - <a href="https://bit.ly/3060ydy">Alan Kay</a></li>
  <li>[3] <a href="https://bit.ly/3j65ruK">Cobra Effect</a></li>
</ul>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="theory" /><category term="devops" /><category term="software" /><category term="engineering" /><summary type="html"><![CDATA[Today I will highlight a few theories that are well related to some of the extents of software engineering. “Software engineering” is a method that divides the whole development process into smaller modules and moves towards the solution through detailed analysis. Each of these small modules is the sum of one or more problems.]]></summary></entry><entry><title type="html">Writing simple unit test case for AWS infrastructure</title><link href="/posts/2019/08/aws-unit-test/" rel="alternate" type="text/html" title="Writing simple unit test case for AWS infrastructure" /><published>2019-08-09T00:00:00+00:00</published><updated>2019-08-09T00:00:00+00:00</updated><id>/posts/2019/08/aws-unit-test</id><content type="html" xml:base="/posts/2019/08/aws-unit-test/"><![CDATA[<p>The more you write code regardless of application or infrastructure, the more you desire to bring the best outcome of your writing when they are running as a realtime entity. And no way to deny that testing helps us to gain our confidence before going for production. So, why leave your infrastructure code without testing? In this blog post, I am going to introduce writing unit tests for your infrastructure in AWS.</p>

<p>Infrastructure as a Code (IAAC) is a blessing to us all, which allows us to code our infrastructure configuration under version control. To reuse a particular piece of code over and over, you have to declare modules just as the same you declare objects in OOP. But what if the base configuration template is not battle-tested or gets prone to configuration drift? Well, in this case, you better implement unit test cases to cross-check your configuration against acceptance criteria.</p>

<h2 id="required-tools">Required Tools</h2>

<ul>
  <li><strong>terraform:</strong> Terraform is a widely used tool for writing infrastructure code and spinning cloud resources.</li>
  <li><strong>kitchen-terraform:</strong> It is a terraform plugin used to test terraform infrastructure configuration</li>
  <li><strong>rspec:</strong> Rspec is testing tool for ruby language which is domain specific in nature</li>
  <li><strong>awsspec:</strong> It is rspec test suite for testing AWS infrastructure resources</li>
  <li><strong>ruby:</strong> Ruby programming language</li>
  <li><strong>bundler:</strong> Manages ruby application dependencies and ruby gems</li>
  <li><strong>git:</strong> Version control</li>
  <li><strong>make:</strong> Linux build tool to execute task in easy way</li>
</ul>

<h2 id="discussion">Discussion</h2>

<p>At first make sure that you have git, ruby, bundler and terraform installed in your system. Then clone <a href="https://github.com/shudarshon/terraform-aws-unit-test">this</a> git repository. This repository holds tiny configurations for testing terraform code against AWS.</p>

<p>From the project structure we see that we got a <code class="language-plaintext highlighter-rouge">main.tf.env</code> file which is actually a terraform configuration file for a single ec2 instance. You need to rename that file to <code class="language-plaintext highlighter-rouge">main.tf</code> and use real values in configuration parameter to make the project work.</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">module</span> <span class="s2">"ec2"</span> <span class="p">{</span>
  <span class="nx">source</span>         <span class="p">=</span> <span class="s2">"modules/ec2"</span>
  <span class="nx">aws_region</span>     <span class="p">=</span> <span class="s2">"xxx"</span>
  <span class="nx">instance_type</span>  <span class="p">=</span> <span class="s2">"xxx"</span>
  <span class="nx">instance_name</span>  <span class="p">=</span> <span class="s2">"xxx"</span>
  <span class="nx">ami_id</span>         <span class="p">=</span> <span class="s2">"xxx"</span>
  <span class="nx">subnet_id</span>      <span class="p">=</span> <span class="s2">"xxx"</span>
  <span class="nx">security_group</span> <span class="p">=</span> <span class="s2">"xxx"</span>
  <span class="nx">ssh_user_name</span>  <span class="p">=</span> <span class="s2">"xxx"</span>
  <span class="nx">ssh_key_name</span>   <span class="p">=</span> <span class="s2">"&lt;key-name&gt;"</span>
  <span class="nx">ssh_key_path</span>   <span class="p">=</span> <span class="s2">"/home/user/keyfiles/&lt;key-name&gt;.pem"</span>
  <span class="nx">instance_count</span> <span class="p">=</span> <span class="mi">1</span>
  <span class="nx">dev_host_label</span> <span class="p">=</span> <span class="s2">"dev"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Terraform provides module reuse features just like OOP. You create a module and later use them for similar type of resources multiple times with their own values. Here terraform module <code class="language-plaintext highlighter-rouge">ec2</code> is declared under <code class="language-plaintext highlighter-rouge">modules</code> directory.</p>

<p>Let’s look at the kitchen test configuration file <code class="language-plaintext highlighter-rouge">kitchen.yml</code>:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">driver</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">terraform"</span>
  <span class="na">root_module_directory</span><span class="pi">:</span> <span class="s2">"</span><span class="s">."</span>

<span class="na">provisioner</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">terraform"</span>

<span class="na">platforms</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">aws"</span>

<span class="na">verifier</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">awspec"</span>

<span class="na">suites</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">default"</span>
    <span class="na">verifier</span><span class="pi">:</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s2">"</span><span class="s">awspec"</span>
      <span class="na">patterns</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">test/unit/ec2/default_test.rb"</span>
</code></pre></div></div>

<p>The test configuration uses <code class="language-plaintext highlighter-rouge">awsspec</code> to verify test cases written at <code class="language-plaintext highlighter-rouge">test/unit/ec2/default_test.rb</code>.</p>

<p>Now let’s see the unit test file. At first we require the base dependencies and declare variables that hold exact values of <code class="language-plaintext highlighter-rouge">instance_name</code> and <code class="language-plaintext highlighter-rouge">security_group</code> from <code class="language-plaintext highlighter-rouge">main.tf</code>:</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># frozen_string_literal: true</span>

<span class="nb">require</span> <span class="s1">'awspec'</span>
<span class="nb">require</span> <span class="s1">'aws-sdk'</span>
<span class="nb">require</span> <span class="s1">'rhcl'</span>

<span class="n">config_main</span> <span class="o">=</span> <span class="no">Rhcl</span><span class="p">.</span><span class="nf">parse</span><span class="p">(</span><span class="no">File</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="s1">'main.tf'</span><span class="p">))</span>
<span class="n">ec2_name</span> <span class="o">=</span> <span class="n">config_main</span><span class="p">[</span><span class="s1">'module'</span><span class="p">][</span><span class="s1">'ec2'</span><span class="p">][</span><span class="s1">'instance_name'</span><span class="p">]</span>
<span class="n">sg_id</span> <span class="o">=</span> <span class="n">config_main</span><span class="p">[</span><span class="s1">'module'</span><span class="p">][</span><span class="s1">'ec2'</span><span class="p">][</span><span class="s1">'security_group'</span><span class="p">]</span>
</code></pre></div></div>

<p>After spinning up resources we need more parameters from the terraform state file:</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state_file</span> <span class="o">=</span> <span class="s1">'./terraform.tfstate.d/kitchen-terraform-default-aws/terraform.tfstate'</span>
<span class="n">tf_state</span> <span class="o">=</span> <span class="no">JSON</span><span class="p">.</span><span class="nf">parse</span><span class="p">(</span><span class="no">File</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="n">state_file</span><span class="p">).</span><span class="nf">read</span><span class="p">)</span>
<span class="n">subnet_id</span> <span class="o">=</span> <span class="n">tf_state</span><span class="p">[</span><span class="s1">'modules'</span><span class="p">][</span><span class="mi">1</span><span class="p">][</span><span class="s1">'resources'</span><span class="p">][</span><span class="s1">'aws_instance.DevInstanceAWS'</span><span class="p">][</span><span class="s1">'primary'</span><span class="p">][</span><span class="s1">'attributes'</span><span class="p">][</span><span class="s1">'subnet_id'</span><span class="p">]</span>
<span class="n">root_volume_id</span> <span class="o">=</span> <span class="n">tf_state</span><span class="p">[</span><span class="s1">'modules'</span><span class="p">][</span><span class="mi">1</span><span class="p">][</span><span class="s1">'resources'</span><span class="p">][</span><span class="s1">'aws_instance.DevInstanceAWS'</span><span class="p">][</span><span class="s1">'primary'</span><span class="p">][</span><span class="s1">'attributes'</span><span class="p">][</span><span class="s1">'root_block_device.0.volume_id'</span><span class="p">]</span>
</code></pre></div></div>

<p>Finally, the test cases written in gherkin syntax compare the EC2 properties against our variables:</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">describe</span> <span class="n">ec2</span><span class="p">(</span><span class="n">ec2_name</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="k">do</span>
  <span class="n">it</span> <span class="p">{</span> <span class="n">should</span> <span class="n">exist</span> <span class="p">}</span>
<span class="k">end</span>

<span class="n">describe</span> <span class="n">ec2</span><span class="p">(</span><span class="n">ec2_name</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="k">do</span>
  <span class="n">it</span> <span class="p">{</span> <span class="n">should</span> <span class="n">be_running</span> <span class="p">}</span>
<span class="k">end</span>

<span class="n">describe</span> <span class="n">ec2</span><span class="p">(</span><span class="n">ec2_name</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="k">do</span>
  <span class="n">it</span> <span class="p">{</span> <span class="n">should</span> <span class="n">belong_to_subnet</span><span class="p">(</span><span class="n">subnet_id</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="p">}</span>
<span class="k">end</span>

<span class="n">describe</span> <span class="n">ec2</span><span class="p">(</span><span class="n">ec2_name</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="k">do</span>
  <span class="n">it</span> <span class="p">{</span> <span class="n">should</span> <span class="n">have_ebs</span><span class="p">(</span><span class="n">root_volume_id</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="p">}</span>
<span class="k">end</span>

<span class="n">describe</span> <span class="n">ec2</span><span class="p">(</span><span class="n">ec2_name</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="k">do</span>
  <span class="n">it</span> <span class="p">{</span> <span class="n">should</span> <span class="n">have_security_group</span><span class="p">(</span><span class="n">sg_id</span><span class="p">.</span><span class="nf">to_s</span><span class="p">)</span> <span class="p">}</span>
<span class="k">end</span>
</code></pre></div></div>

<h2 id="how-to-run">How to run</h2>

<ol>
  <li>Copy <code class="language-plaintext highlighter-rouge">main.tf.env</code> to <code class="language-plaintext highlighter-rouge">main.tf</code> and fill in terraform variable values</li>
  <li>Install testing dependencies: <code class="language-plaintext highlighter-rouge">bundle install --path vendor/bundle</code></li>
  <li>Kitchen converge (spins up resources): <code class="language-plaintext highlighter-rouge">bundle exec kitchen converge</code></li>
  <li>Verify infrastructure: <code class="language-plaintext highlighter-rouge">bundle exec kitchen verify</code></li>
  <li>Destroy resources when done: <code class="language-plaintext highlighter-rouge">bundle exec kitchen destroy</code></li>
</ol>

<p>If you find any issue create an issue <a href="https://github.com/shudarshon/terraform-aws-unit-test">here</a>.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="testing" /><category term="aws" /><category term="terraform" /><summary type="html"><![CDATA[The more you write code regardless of application or infrastructure, the more you desire to bring the best outcome of your writing when they are running as a realtime entity. And no way to deny that testing helps us to gain our confidence before going for production. So, why leave your infrastructure code without testing? In this blog post, I am going to introduce writing unit tests for your infrastructure in AWS.]]></summary></entry><entry><title type="html">Writing incident postmortem to learn from previous history and avoid blaming</title><link href="/posts/2019/07/devops-post-mortem/" rel="alternate" type="text/html" title="Writing incident postmortem to learn from previous history and avoid blaming" /><published>2019-07-31T00:00:00+00:00</published><updated>2019-07-31T00:00:00+00:00</updated><id>/posts/2019/07/devops-post-mortem</id><content type="html" xml:base="/posts/2019/07/devops-post-mortem/"><![CDATA[<p>Errors are sneaky, often remain hidden, smart and somehow get past no matter they are caused by any human action or any prevailing bug. This error often causes incidents in the running system causing a great negative impact on the team. To avoid the same mistakes in the future it is needed to document them properly which is often referred to as incident postmortem writing.</p>

<p>As an operation/reliability engineer, you are already well introduced to service interruption and you need to shift your focus on quickly recovering the situation. What if you solve the problem and restore team discipline by solving this crisis in a short time? Do you think enough about mitigating the same problem again and again in the future? What if you forget the service recovery procedure over time by busywork or any team shift? Well, this is the time you offer yourself writing post mortem report which is a great blessing to your future self or any new engineer assigned for the same task.</p>

<p>So let’s discuss on contents of postmortems. This report consists of some sections which are very specific to its purpose. Such as incident date, Author, Status, Summary, Impact, Root Cause, Trigger, Resolution, Detection, Proof of Concept for future mitigation, Supporting information and lastly people involved behind the solution of the crisis. A sample post mortem report would look like:</p>

<h2 id="date">Date</h2>
<p>July 29th, 2019</p>

<h2 id="author">Author</h2>
<p>DevOps Guy (<a href="mailto:devops@example.com">devops@example.com</a>)</p>

<h2 id="status">Status</h2>
<p>Resolved</p>

<h2 id="summary">Summary</h2>
<p>X application (hello.example.com) was down for 5 minutes 9 seconds. Due to SQL error in the database constraint in liquibase settings residing under source code. During the error time application was responding with HTTP error 404.</p>
<ul>
  <li>Incident Occurrence Time: 11:57 A.M. (UTC + 6:00)</li>
  <li>Investigation Starts: 11:58 A.M. (UTC + 6:00)</li>
  <li>Investigation Finished: 12:03 P.M. (UTC + 6:00)</li>
</ul>

<h2 id="impact">Impact</h2>
<p>This outage impacted SRE team to test features on staging application. No real customer was harmed.</p>

<h2 id="root-causes">Root Causes</h2>
<p>Source code changes resulted in automated build which made liquibase to perform some changes in database but it was violating database constraint which resulted in database/backend error causing application to crash.</p>

<h2 id="trigger">Trigger</h2>
<p>The outage was caused after the push to source code with commit id xyzxyz. This commit eventually built S3 bucket build artifact with version X. New artifact got deployed into server and occurred outage.</p>

<h2 id="resolution">Resolution</h2>
<p>The outage was fixed after the push to source code with commit id zxyzxy. This commit eventually built S3 bucket build artifact with version Y. New artifact got deployed and fixed outage.</p>

<h2 id="detection">Detection</h2>
<p>The problem was noticed by team SRE first. Later we got automated crash report in slack with email from our monitoring agent.</p>

<h2 id="poc-for-future-mitigation">PoC for Future Mitigation</h2>
<ul>
  <li>Need to implement database version controlling with application specific branching</li>
  <li>Need to implement blue green deployment in application</li>
  <li>Need to reduce build time to deliver software earlier</li>
</ul>

<hr />

<p>Remember that a good team is better than the best when good intentions of team members are appreciated in parallel with their hard work. However, by nature, we are prone to error and in a team perspective, this might somehow result in chaos. During the time of service downtime crisis, the meaningful approaches would be covering up mistakes, bringing the discipline back and documenting the mistakes for better learning and simulating for future avoidance. And through writing postmortems, it is possible to bring out the maximum value of the team in the future by the removal of casting blame to one another. So say no to repeated mistakes!</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="devops" /><category term="sre" /><category term="incident" /><category term="postmortem" /><summary type="html"><![CDATA[Errors are sneaky, often remain hidden, smart and somehow get past no matter they are caused by any human action or any prevailing bug. This error often causes incidents in the running system causing a great negative impact on the team. To avoid the same mistakes in the future it is needed to document them properly which is often referred to as incident postmortem writing.]]></summary></entry><entry><title type="html">Using S3 bucket as Terraform Remote Backend</title><link href="/posts/2019/03/terraform-s3-backend/" rel="alternate" type="text/html" title="Using S3 bucket as Terraform Remote Backend" /><published>2019-03-16T00:00:00+00:00</published><updated>2019-03-16T00:00:00+00:00</updated><id>/posts/2019/03/terraform-s3-backend</id><content type="html" xml:base="/posts/2019/03/terraform-s3-backend/"><![CDATA[<p>If you are familiar with IaC then you must have heard about terraform, right? Terraform is an awesome tool letting you to create, destroy, provision and configure your infrastructure as code. When terraform is instructed to run against some of its configuration then it creates a state file in the developer machine. This state file contains infrastructure configuration as applied by Terraform. But if you want to work on the terraform codebase as a team allowing terraform to create state file locally then every member will be in a great dilemma. Its because the other developers would be also requiring access to this file. Well you might think that we can use git, right? But one problem with that is some sensitive fields such as credentials might get exposed and this is not what you want. One solution is to use terraform S3 backend for storing infrastructure state in S3 bucket.</p>

<h2 id="requirements">Requirements</h2>

<ul>
  <li>AWS CLI Access</li>
  <li>Terraform</li>
  <li>Make (build tool)</li>
</ul>

<h2 id="procedure">Procedure</h2>

<p>At first, export AWS access key id and secret key to environment variables which will be used by Terraform.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">export </span><span class="nv">AWS_ACCESS_KEY_ID</span><span class="o">=</span><span class="s2">"&lt;access-key&gt;"</span>
<span class="nv">$ </span><span class="nb">export </span><span class="nv">AWS_SECRET_ACCESS_KEY</span><span class="o">=</span><span class="s2">"&lt;secret-key&gt;"</span>
</code></pre></div></div>

<p>Since S3 is our choice for using remote backend so create an S3 bucket for storing terraform remote state file.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>aws s3api create-bucket <span class="nt">--bucket</span> webapp-terraform-state <span class="nt">--region</span> us-east-1
</code></pre></div></div>

<p>Next, clone this repository and get into specific source code repository.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git clone https://github.com/shudarshon/challenge-terraform.git
<span class="nv">$ </span><span class="nb">cd </span>challenge-terraform/6-tf-s3-backend/vpc
</code></pre></div></div>

<p>From the source code we can see that there are 4 different AWS resources terraform configuration categorized into specific folder by resource name. Lets find out how a terraform remote backend configuration (vpc/backend.tf) looks like:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">terraform</span> <span class="p">{</span>
  <span class="nx">backend</span> <span class="s2">"s3"</span> <span class="p">{</span>
    <span class="nx">bucket</span>  <span class="p">=</span> <span class="s2">"webapp-terraform-state"</span>
    <span class="nx">key</span>     <span class="p">=</span> <span class="s2">"vpc/vpc.tfstate"</span>
    <span class="nx">region</span>  <span class="p">=</span> <span class="s2">"us-east-1"</span>
    <span class="nx">encrypt</span> <span class="p">=</span> <span class="kc">true</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>In above configuration we have specified the bucket name along with the S3 key. We need to specify the region of S3 bucket also. Generally, it is a good practice to isolate the resource specific state file into separate folder under S3 bucket.</p>

<p>Writing the backend configuration is not enough. We need also save the important resource properties in remote state file. For instance, suppose we need to allow VPC CIDR block traffic in a specific security group then we need to use <code class="language-plaintext highlighter-rouge">terraform output</code> to save that configuration in remote state:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">output</span> <span class="s2">"vpc_id"</span> <span class="p">{</span>
  <span class="nx">value</span> <span class="p">=</span> <span class="s2">"${aws_vpc.vpc.id}"</span>
<span class="p">}</span>

<span class="nx">output</span> <span class="s2">"vpc_cidr"</span> <span class="p">{</span>
  <span class="nx">value</span> <span class="p">=</span> <span class="s2">"${aws_vpc.vpc.cidr_block}"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Now we need to initialize this configuration with terraform and make it create a VPC resource in AWS:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>make init
<span class="nv">$ </span>make apply
</code></pre></div></div>

<p>Now lets create security group in that specific VPC. For that, we need to introduce VPC remote state file in security group remote backend configuration and use that backend information as a data source:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">data</span> <span class="s2">"terraform_remote_state"</span> <span class="s2">"shared_vpc_local"</span> <span class="p">{</span>
  <span class="nx">backend</span> <span class="p">=</span> <span class="s2">"s3"</span>
  <span class="nx">config</span> <span class="p">{</span>
    <span class="nx">bucket</span> <span class="p">=</span> <span class="s2">"webapp-terraform-state"</span>
    <span class="nx">key</span>    <span class="p">=</span> <span class="s2">"vpc/vpc.tfstate"</span>
    <span class="nx">region</span> <span class="p">=</span> <span class="s2">"us-east-1"</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Then use that data source for retrieving <code class="language-plaintext highlighter-rouge">vpc_cidr</code> property from remote state file:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">egress</span> <span class="p">{</span>
  <span class="nx">from_port</span>   <span class="p">=</span> <span class="mi">80</span>
  <span class="nx">to_port</span>     <span class="p">=</span> <span class="mi">80</span>
  <span class="nx">protocol</span>    <span class="p">=</span> <span class="s2">"tcp"</span>
  <span class="nx">cidr_blocks</span> <span class="p">=</span> <span class="p">[</span><span class="s2">"${data.terraform_remote_state.shared_vpc_local.vpc_cidr}"</span><span class="p">]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Use <code class="language-plaintext highlighter-rouge">make apply</code> to run the configuration and you will find that security groups got created with desired configuration. Get the source code <a href="https://github.com/shudarshon/challenge-terraform/tree/master/6-tf-s3-backend">here</a>.</p>

<p>If you find any issue create an issue <a href="https://github.com/shudarshon/challenge-terraform/issues">here</a>.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="iaac" /><category term="terraform" /><category term="s3" /><summary type="html"><![CDATA[If you are familiar with IaC then you must have heard about terraform, right? Terraform is an awesome tool letting you to create, destroy, provision and configure your infrastructure as code. When terraform is instructed to run against some of its configuration then it creates a state file in the developer machine. This state file contains infrastructure configuration as applied by Terraform. But if you want to work on the terraform codebase as a team allowing terraform to create state file locally then every member will be in a great dilemma. Its because the other developers would be also requiring access to this file. Well you might think that we can use git, right? But one problem with that is some sensitive fields such as credentials might get exposed and this is not what you want. One solution is to use terraform S3 backend for storing infrastructure state in S3 bucket.]]></summary></entry><entry><title type="html">Securing Wordpress Application Secret and Managing Jenkins Pipeline with EC2 Parameter Store</title><link href="/posts/2018/08/jenkins-secret-parameter-store/" rel="alternate" type="text/html" title="Securing Wordpress Application Secret and Managing Jenkins Pipeline with EC2 Parameter Store" /><published>2018-08-19T00:00:00+00:00</published><updated>2018-08-19T00:00:00+00:00</updated><id>/posts/2018/08/jenkins-secret-parameter-store</id><content type="html" xml:base="/posts/2018/08/jenkins-secret-parameter-store/"><![CDATA[<p>After installing Wordpress when you setup application it will create a file named <code class="language-plaintext highlighter-rouge">wp-config.php</code> in the root of the source code. This file contains sensitive informations like database url, username, password and database name. We need mask those values with fake one and replace them with original one before build process. In this post, I will discuss on keeping these values secret with EC2 parameter store and using them in Jenkins pipeline.</p>

<p>However, you can also use HashiCorp’s Vault to manage secrets which is also very popular. But you need to manage that on your own. Now, lets focus on AWS Parameter Store service and see its advantages:</p>

<ul>
  <li>Isolates sensitive data from source control</li>
  <li>Enables to track version control of parameter store variables</li>
  <li>Easily integrates with various AWS services</li>
</ul>

<h2 id="requirements">Requirements</h2>

<ul>
  <li>AWS CLI Access (note the region you have configured)</li>
  <li>IAM policies to allow to fetch value from AWS parameter store</li>
</ul>

<h2 id="procedure">Procedure</h2>

<p>First we will create an IAM role with <code class="language-plaintext highlighter-rouge">AmazonEC2RoleforSSM</code> policy attached. We will bind this role with EC2 instance where Jenkins slave will be running.</p>

<p>Next, create an EC2 instance with above IAM role and install <code class="language-plaintext highlighter-rouge">awscli</code>. Next, use <code class="language-plaintext highlighter-rouge">aws configure</code> command and configure only the region where we are going to save secrets in Parameter Store.</p>

<p>The <code class="language-plaintext highlighter-rouge">wp-config.php</code> contains four sensitive informations such as <code class="language-plaintext highlighter-rouge">DB_NAME</code>, <code class="language-plaintext highlighter-rouge">DB_USER</code>, <code class="language-plaintext highlighter-rouge">DB_PASSWORD</code> &amp; <code class="language-plaintext highlighter-rouge">DB_HOST</code>. We need to replace the real values with the fake values and then make a git commit.</p>

<p>Now, put secrets in Parameter store:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>aws ssm put-parameter <span class="nt">--name</span> <span class="s2">"DATABASE_NAME"</span> <span class="nt">--type</span> <span class="s2">"String"</span> <span class="nt">--value</span> <span class="s2">"&lt;real-database-name&gt;"</span>
<span class="nv">$ </span>aws ssm put-parameter <span class="nt">--name</span> <span class="s2">"DB_USERNAME"</span> <span class="nt">--type</span> <span class="s2">"String"</span> <span class="nt">--value</span> <span class="s2">"&lt;real-username&gt;"</span>
<span class="nv">$ </span>aws ssm put-parameter <span class="nt">--name</span> <span class="s2">"DB_PASSWORD"</span> <span class="nt">--type</span> <span class="s2">"String"</span> <span class="nt">--value</span> <span class="s2">"&lt;real-password&gt;"</span>
<span class="nv">$ </span>aws ssm put-parameter <span class="nt">--name</span> <span class="s2">"DB_URL"</span> <span class="nt">--type</span> <span class="s2">"String"</span> <span class="nt">--value</span> <span class="s2">"&lt;real-db-endpoint&gt;"</span>
</code></pre></div></div>

<p>For extra layer of security you can also use AWS KMS to encrypt these values.</p>

<p>Then write a Jenkins pipeline script which fetches the secrets and replaces <code class="language-plaintext highlighter-rouge">wp-config.php</code> with original values just before the build happens:</p>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
  <span class="n">agent</span> <span class="n">any</span>
  <span class="n">environment</span> <span class="o">{</span>
    <span class="n">DB_USERNAME</span> <span class="o">=</span> <span class="s1">'$(aws ssm get-parameters --region us-east-1 --names DB_USERNAME --query Parameters[0].Value | tr -d \'"\')'</span>
    <span class="n">DATABASE_NAME</span> <span class="o">=</span> <span class="s1">'$(aws ssm get-parameters --region us-east-1 --names DATABASE_NAME --query Parameters[0].Value | tr -d \'"\')'</span>
    <span class="n">DB_PASSWORD</span> <span class="o">=</span> <span class="s1">'$(aws ssm get-parameters --region us-east-1 --names DB_PASSWORD --query Parameters[0].Value | tr -d \'"\')'</span>
    <span class="n">DB_URL</span> <span class="o">=</span> <span class="s1">'$(aws ssm get-parameters --region us-east-1 --names DB_URL --query Parameters[0].Value | tr -d \'"\')'</span>
  <span class="o">}</span>
  <span class="n">stages</span> <span class="o">{</span>
    <span class="n">stage</span><span class="o">(</span><span class="s1">'Prepare'</span><span class="o">)</span> <span class="o">{</span>
      <span class="n">steps</span> <span class="o">{</span>
        <span class="n">sh</span> <span class="s2">"sed -i \"/DB_NAME/s/'[^']*'/'${DATABASE_NAME}'/2\" wp-config.php"</span>
        <span class="n">sh</span> <span class="s2">"sed -i \"/DB_USER/s/'[^']*'/'${DB_USERNAME}'/2\" wp-config.php"</span>
        <span class="n">sh</span> <span class="s2">"sed -i \"/DB_PASSWORD/s/'[^']*'/'${DB_PASSWORD}'/2\" wp-config.php"</span>
        <span class="n">sh</span> <span class="s2">"sed -i \"/DB_HOST/s/'[^']*'/'${DB_URL}'/2\" wp-config.php"</span>
      <span class="o">}</span>
    <span class="o">}</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Keep this <code class="language-plaintext highlighter-rouge">Jenkinsfile</code> in the root of the source code and perform git commit followed by a Jenkins build. Make sure to delete the workspace after the build has been finished.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="jenkins" /><category term="security" /><category term="aws" /><summary type="html"><![CDATA[After installing Wordpress when you setup application it will create a file named wp-config.php in the root of the source code. This file contains sensitive informations like database url, username, password and database name. We need mask those values with fake one and replace them with original one before build process. In this post, I will discuss on keeping these values secret with EC2 parameter store and using them in Jenkins pipeline.]]></summary></entry><entry><title type="html">Infrastructure provisioning and configuration management with AWS, Terraform and Ansible</title><link href="/posts/2018/07/terraform-ansible/" rel="alternate" type="text/html" title="Infrastructure provisioning and configuration management with AWS, Terraform and Ansible" /><published>2018-07-27T00:00:00+00:00</published><updated>2018-07-27T00:00:00+00:00</updated><id>/posts/2018/07/terraform-ansible</id><content type="html" xml:base="/posts/2018/07/terraform-ansible/"><![CDATA[<p>Regularly a DevOps has to perform different types of server provisioning, configuration with different architectures for deployment testing, applying and testing IaaC modules or for building dev/stage environment. Terraform is such a tool which helps us to build &amp; manage infrastructure using different cloud vendor API. Ansible is another tool to automate infrastructure configuration changes. In this post, I will discuss on using Terraform and Ansible together to bring infrastructure in desired state quickly.</p>

<p>To simplify deployment we need to often use golden machine images which will have all of its configuration tested, patched and updated to stable version. To create such machine images we can use these two tools combined.</p>

<h2 id="requirements">Requirements</h2>

<ul>
  <li>AWS CLI Access</li>
  <li>Terraform</li>
  <li>Ansible</li>
  <li>Git</li>
</ul>

<h2 id="procedure">Procedure</h2>

<p>At first, export AWS access key id and secret key to environment variables:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">export </span><span class="nv">AWS_ACCESS_KEY_ID</span><span class="o">=</span><span class="s2">"&lt;access-key&gt;"</span>
<span class="nv">$ </span><span class="nb">export </span><span class="nv">AWS_SECRET_ACCESS_KEY</span><span class="o">=</span><span class="s2">"&lt;secret-key&gt;"</span>
</code></pre></div></div>

<p>Next, clone the repository:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>git clone https://github.com/shudarshon/challenge-terraform.git
<span class="nv">$ </span><span class="nb">cd </span>1-terra-ansible
</code></pre></div></div>

<p>We collect SSH username, remote host IP address in Ansible host file while Terraform creates the resources using <code class="language-plaintext highlighter-rouge">null_resource</code>:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">resource</span> <span class="s2">"null_resource"</span> <span class="s2">"ConfigureAnsibleLabelVariable"</span> <span class="p">{</span>
  <span class="nx">provisioner</span> <span class="s2">"local-exec"</span> <span class="p">{</span>
    <span class="nx">command</span> <span class="p">=</span> <span class="s2">"echo [${var.dev_host_label}:vars] &gt; hosts"</span>
  <span class="p">}</span>
  <span class="nx">provisioner</span> <span class="s2">"local-exec"</span> <span class="p">{</span>
    <span class="nx">command</span> <span class="p">=</span> <span class="s2">"echo ansible_ssh_user=${var.ssh_user_name} &gt;&gt; hosts"</span>
  <span class="p">}</span>
  <span class="nx">provisioner</span> <span class="s2">"local-exec"</span> <span class="p">{</span>
    <span class="nx">command</span> <span class="p">=</span> <span class="s2">"echo ansible_ssh_private_key_file=${var.ssh_key_path} &gt;&gt; hosts"</span>
  <span class="p">}</span>
  <span class="nx">provisioner</span> <span class="s2">"local-exec"</span> <span class="p">{</span>
    <span class="nx">command</span> <span class="p">=</span> <span class="s2">"echo [${var.dev_host_label}] &gt;&gt; hosts"</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>While creating instances we provision them with essential packages and gather public IP addresses:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">resource</span> <span class="s2">"null_resource"</span> <span class="s2">"ProvisionRemoteHostsIpToAnsibleHosts"</span> <span class="p">{</span>
  <span class="nx">count</span> <span class="p">=</span> <span class="s2">"${var.instance_count}"</span>
  <span class="nx">connection</span> <span class="p">{</span>
    <span class="nx">type</span>        <span class="p">=</span> <span class="s2">"ssh"</span>
    <span class="nx">user</span>        <span class="p">=</span> <span class="s2">"${var.ssh_user_name}"</span>
    <span class="nx">host</span>        <span class="p">=</span> <span class="s2">"${element(aws_instance.DevInstanceAWS.*.public_ip, count.index)}"</span>
    <span class="nx">private_key</span> <span class="p">=</span> <span class="s2">"${file("</span><span class="nx">$</span><span class="p">{</span><span class="nx">var</span><span class="err">.</span><span class="nx">ssh_key_path</span><span class="p">}</span><span class="s2">")}"</span>
  <span class="p">}</span>
  <span class="nx">provisioner</span> <span class="s2">"remote-exec"</span> <span class="p">{</span>
    <span class="nx">inline</span> <span class="p">=</span> <span class="p">[</span>
      <span class="s2">"sudo yum update -y"</span><span class="p">,</span>
      <span class="s2">"sudo yum install python-setuptools python-pip -y"</span><span class="p">,</span>
      <span class="s2">"sudo pip install httplib2"</span>
    <span class="p">]</span>
  <span class="p">}</span>
  <span class="nx">provisioner</span> <span class="s2">"local-exec"</span> <span class="p">{</span>
    <span class="nx">command</span> <span class="p">=</span> <span class="s2">"echo ${element(aws_instance.DevInstanceAWS.*.public_ip, count.index)} &gt;&gt; hosts"</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Then we run the Ansible playbook from Terraform:</p>

<div class="language-hcl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">resource</span> <span class="s2">"null_resource"</span> <span class="s2">"ModifyApplyAnsiblePlayBook"</span> <span class="p">{</span>
  <span class="nx">provisioner</span> <span class="s2">"local-exec"</span> <span class="p">{</span>
    <span class="nx">command</span> <span class="p">=</span> <span class="s2">"sed -i -e '/hosts:/ s/: .*/: ${var.dev_host_label}/' play.yml"</span>
  <span class="p">}</span>
  <span class="nx">provisioner</span> <span class="s2">"local-exec"</span> <span class="p">{</span>
    <span class="nx">command</span> <span class="p">=</span> <span class="s2">"sleep 10; ansible-playbook -i hosts play.yml"</span>
  <span class="p">}</span>
  <span class="nx">depends_on</span> <span class="p">=</span> <span class="p">[</span><span class="s2">"null_resource.ProvisionRemoteHostsIpToAnsibleHosts"</span><span class="p">]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Run the following to apply:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>terraform init
<span class="nv">$ </span>terraform plan
<span class="nv">$ </span>terraform apply
</code></pre></div></div>

<p>If you find any issue create an issue <a href="https://github.com/shudarshon/challenge-terraform/issues">here</a>.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="iaac" /><category term="terraform" /><category term="ansible" /><category term="aws" /><summary type="html"><![CDATA[Regularly a DevOps has to perform different types of server provisioning, configuration with different architectures for deployment testing, applying and testing IaaC modules or for building dev/stage environment. Terraform is such a tool which helps us to build &amp; manage infrastructure using different cloud vendor API. Ansible is another tool to automate infrastructure configuration changes. In this post, I will discuss on using Terraform and Ansible together to bring infrastructure in desired state quickly.]]></summary></entry><entry><title type="html">Managing Docker Secret with AWS Parameter Store</title><link href="/posts/2018/07/docker-secret-parameter-store/" rel="alternate" type="text/html" title="Managing Docker Secret with AWS Parameter Store" /><published>2018-07-21T00:00:00+00:00</published><updated>2018-07-21T00:00:00+00:00</updated><id>/posts/2018/07/docker-secret-parameter-store</id><content type="html" xml:base="/posts/2018/07/docker-secret-parameter-store/"><![CDATA[<p>We write Dockerfile for packaging our application into Docker images. Dockerfile is a set of plaintext instructions with predeclared environment variables. These environment variables contain sensitive information such as username, password, remote database URL, bastion host IP etc. When secret variables will be visible in source code repository then code repository members can see them easily. Obviously, we need to tackle this situation! In this post, I will discuss on keeping these values secret in AWS, fetching and using them in docker runtime.</p>

<p>AWS Parameter Store service advantages:</p>

<ul>
  <li>Isolates sensitive data from source control</li>
  <li>Enables to track version control of parameter store variables</li>
  <li>Easily integrates with various AWS services</li>
</ul>

<h2 id="requirements">Requirements</h2>

<ul>
  <li>AWS CLI Access</li>
  <li>jq</li>
  <li>IAM policies to allow to fetch value from AWS parameter store</li>
  <li>Docker with sudoer user’s group membership</li>
  <li>Dockerfile</li>
</ul>

<h2 id="procedure">Procedure</h2>

<p>First we will create an IAM role with <code class="language-plaintext highlighter-rouge">AmazonEC2RoleforSSM</code> policy attached and bind this role with the EC2 instance where Docker will be running.</p>

<p>Next, put secrets in Parameter store:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>aws ssm put-parameter <span class="nt">--name</span> <span class="s2">"SONARQUBE_JDBC_USERNAME"</span> <span class="nt">--type</span> <span class="s2">"String"</span> <span class="nt">--value</span> <span class="s2">"&lt;actual-username&gt;"</span>
<span class="nv">$ </span>aws ssm put-parameter <span class="nt">--name</span> <span class="s2">"SONARQUBE_JDBC_PASSWORD"</span> <span class="nt">--type</span> <span class="s2">"String"</span> <span class="nt">--value</span> <span class="s2">"&lt;actual-password&gt;"</span>
<span class="nv">$ </span>aws ssm put-parameter <span class="nt">--name</span> <span class="s2">"SONARQUBE_JDBC_URL"</span> <span class="nt">--type</span> <span class="s2">"String"</span> <span class="nt">--value</span> <span class="s2">"jdbc:postgresql://&lt;actual-db-endpoint&gt;/sonar"</span>
</code></pre></div></div>

<p>Now create a bash script alongside the Dockerfile for automatically triggering the value change in build pipeline:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="si">$(</span>aws ssm get-parameters <span class="nt">--names</span> <span class="s2">"SONARQUBE_JDBC_USERNAME"</span> | jq <span class="nt">-r</span> <span class="s1">'.Parameters| .[] | "export " + .Name + "=" + .Value + ""'</span><span class="si">)</span>
<span class="si">$(</span>aws ssm get-parameters <span class="nt">--names</span> <span class="s2">"SONARQUBE_JDBC_PASSWORD"</span> | jq <span class="nt">-r</span> <span class="s1">'.Parameters| .[] | "export " + .Name + "=" + .Value + ""'</span><span class="si">)</span>
<span class="si">$(</span>aws ssm get-parameters <span class="nt">--names</span> <span class="s2">"SONARQUBE_JDBC_URL"</span> | jq <span class="nt">-r</span> <span class="s1">'.Parameters| .[] | "export " + .Name + "=" + .Value + ""'</span><span class="si">)</span>

<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#SONARQUBE_JDBC_USERNAME=[^ ]*#SONARQUBE_JDBC_USERNAME=</span><span class="k">${</span><span class="nv">SONARQUBE_JDBC_USERNAME</span><span class="k">}</span><span class="s2">#"</span> Dockerfile
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#SONARQUBE_JDBC_PASSWORD=[^ ]*#SONARQUBE_JDBC_PASSWORD=</span><span class="k">${</span><span class="nv">SONARQUBE_JDBC_PASSWORD</span><span class="k">}</span><span class="s2">#"</span> Dockerfile
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#SONARQUBE_JDBC_URL=[^ ]*#SONARQUBE_JDBC_URL=</span><span class="k">${</span><span class="nv">SONARQUBE_JDBC_URL</span><span class="k">}</span><span class="s2">#"</span> Dockerfile

docker build <span class="nt">-t</span> app <span class="nb">.</span>
docker run <span class="nt">-d</span> <span class="nt">--name</span> sonarqube <span class="nt">-p</span> 9000:9000 <span class="nt">-p</span> 9092:9092 <span class="nt">--restart</span> unless-stopped app
</code></pre></div></div>

<p>Now if you check the Dockerfile it will be containing new values which we have saved in the AWS Parameter Store.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="docker" /><category term="security" /><category term="aws" /><summary type="html"><![CDATA[We write Dockerfile for packaging our application into Docker images. Dockerfile is a set of plaintext instructions with predeclared environment variables. These environment variables contain sensitive information such as username, password, remote database URL, bastion host IP etc. When secret variables will be visible in source code repository then code repository members can see them easily. Obviously, we need to tackle this situation! In this post, I will discuss on keeping these values secret in AWS, fetching and using them in docker runtime.]]></summary></entry><entry><title type="html">Integrating Jenkins Pipeline with AWS CodeBuild</title><link href="/posts/2018/07/jenkins-codebuild/" rel="alternate" type="text/html" title="Integrating Jenkins Pipeline with AWS CodeBuild" /><published>2018-07-05T00:00:00+00:00</published><updated>2018-07-05T00:00:00+00:00</updated><id>/posts/2018/07/jenkins-codebuild</id><content type="html" xml:base="/posts/2018/07/jenkins-codebuild/"><![CDATA[<p>Jenkins is a task server which is highly used in performing CI/CD jobs. AWS CodeBuild is a service for performing source code compilation, build, testing and artifact generation for deployment. We can integrate these two services to scale our build, reduce build time, minimize billing and exploit the combined power of CodeBuild and Jenkins.</p>

<p>CodeBuild is an excellent tool to perform build and test jobs. But you need to write many AWS Lambda functions with SNS services to notify pre-build and post-build notifications. On the contrary, Jenkins plugins and shared library makes it easy for you to manage notifications. But managing jenkins slaves are painful, costly and they need to be configured with lots of parameters. As a DevOps you need to achieve several CI/CD goals for faster software release in a cost effective way. Thats exactly where we will be using Jenkins with CodeBuild.</p>

<h2 id="requirements">Requirements</h2>

<ul>
  <li>AWS Account ID</li>
  <li>AWS CLI Access (with Full Access privilege)</li>
  <li>Jenkins Server with CodeBuild plugins installed</li>
  <li>Source Code Repository (GitHub/BitBucket/CodeCommit)</li>
  <li>CodeBuild Region information</li>
  <li>Clone this <a href="https://github.com/shudarshon/challenge-jenkins">repository</a></li>
</ul>

<h2 id="procedure">Procedure</h2>

<p>First, create IAM group and user with <code class="language-plaintext highlighter-rouge">AmazonS3FullAccess</code> (optional) and <code class="language-plaintext highlighter-rouge">AWSCodeBuildDeveloperAccess</code> policies. User should be given programmatic access.</p>

<p>Next, create IAM role which will allow CodeBuild to assume policy specified user privileges:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cd </span>7-jenkins-codebuild/iam-codebuild-role-policy/
<span class="nv">$ </span>aws iam create-role <span class="nt">--role-name</span> CodeBuildServiceRole <span class="nt">--assume-role-policy-document</span> file://create-role.json
<span class="nv">$ </span>aws iam put-role-policy <span class="nt">--role-name</span> CodeBuildServiceRole <span class="nt">--policy-name</span> CodeBuildServiceRolePolicy <span class="nt">--policy-document</span> file://put-role-policy.json
</code></pre></div></div>

<p>If you forget AWS Account ID then retrieve it by:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>aws sts get-caller-identity <span class="nt">--output</span> text <span class="nt">--query</span> <span class="s1">'Account'</span>
</code></pre></div></div>

<p>After creating the IAM role, create Jenkins credential with <code class="language-plaintext highlighter-rouge">CodeBuild Credentials type</code> and provide the ARN of IAM role.</p>

<p>Configure CodeBuild from AWS console with SCM source, artifact S3 bucket (optional), specific IAM role and put <code class="language-plaintext highlighter-rouge">buildspec.yml</code> in project root directory along with Jenkinsfile:</p>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">#</span><span class="o">!</span><span class="s">/usr/</span><span class="n">bin</span><span class="o">/</span><span class="n">env</span> <span class="n">groovy</span>

<span class="kn">import</span> <span class="nn">jenkins.model.*</span>

<span class="n">node</span> <span class="o">{</span>
  <span class="n">stage</span><span class="o">(</span><span class="s1">'Build'</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">awsCodeBuild</span> <span class="nl">projectName:</span> <span class="s2">"$CODEBUILD_PROJECT_NAME"</span><span class="o">,</span> <span class="nl">region:</span> <span class="s2">"$REGION"</span><span class="o">,</span> <span class="nl">sourceControlType:</span> <span class="s2">"project"</span><span class="o">,</span> <span class="nl">credentialsId:</span> <span class="s2">"$JENKINS_CREDENTIAL_ID"</span><span class="o">,</span> <span class="nl">credentialsType:</span> <span class="s2">"jenkins"</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>If your project uses remote database (RDS) then you need to allow CodeBuild IP address in firewall rule. Gather CodeBuild service IP range from <a href="https://ip-ranges.amazonaws.com/ip-ranges.json">here</a>.</p>

<p>If you find any issue create an issue <a href="https://github.com/shudarshon/challenge-jenkins/tree/master/7-jenkins-codebuild">here</a>.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="ci/cd" /><category term="jenkins" /><category term="aws" /><summary type="html"><![CDATA[Jenkins is a task server which is highly used in performing CI/CD jobs. AWS CodeBuild is a service for performing source code compilation, build, testing and artifact generation for deployment. We can integrate these two services to scale our build, reduce build time, minimize billing and exploit the combined power of CodeBuild and Jenkins.]]></summary></entry><entry><title type="html">Performing application build and remote deployment with Travis CI</title><link href="/posts/2018/06/build-with-travis/" rel="alternate" type="text/html" title="Performing application build and remote deployment with Travis CI" /><published>2018-06-07T00:00:00+00:00</published><updated>2018-06-07T00:00:00+00:00</updated><id>/posts/2018/06/build-with-travis</id><content type="html" xml:base="/posts/2018/06/build-with-travis/"><![CDATA[<p>Travis CI is another popular CI/CD service which is free for open source projects with limited functionalities. In the free version, unlike Jenkins, Travis CI builds application in a dockerized environment. Travis CI comes with single YAML configuration file where we need specify parameters and configure them to perform successful build and remote deployment.</p>

<p>At first, we will create a <code class="language-plaintext highlighter-rouge">.travis.yml</code> file and <code class="language-plaintext highlighter-rouge">.travis</code> directory in the root of source code repository. After creating the file and directory we need to install Travis client in localhost:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>gem <span class="nb">install </span>travis
travis login
</code></pre></div></div>

<p>While using Travis CI we need to keep the public-private key, remote server IP, SSH username and deploy path encrypted:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /path/to/repository
travis encrypt <span class="nv">DEPLOY_HOST</span><span class="o">=</span>X.X.X.X <span class="nt">--add</span>
travis encrypt <span class="nv">DEPLOY_PATH</span><span class="o">=</span>/home/user/app <span class="nt">--add</span>
travis encrypt <span class="nv">DEPLOY_USER</span><span class="o">=</span>user <span class="nt">--add</span>
</code></pre></div></div>

<p>Now generate SSH keypair and encrypt the private key:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> rsa <span class="nt">-b</span> 4096 <span class="nt">-C</span> <span class="s1">'&lt;email&gt;'</span> <span class="nt">-N</span> <span class="s1">''</span> <span class="nt">-f</span> ./deploy_rsa
travis encrypt-file deploy_rsa <span class="nt">--add</span>
<span class="nb">mv </span>deploy_rsa.enc .travis/
</code></pre></div></div>

<p>Add the public key to the remote server’s <code class="language-plaintext highlighter-rouge">authorized_keys</code> file:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> ./deploy_rsa.pub | ssh <span class="nt">-i</span> ~/.key/test.pem DEPLOY_USER@DEPLOY_HOST <span class="s2">"cat - &gt;&gt; ~/.ssh/authorized_keys"</span>
<span class="nb">rm</span> <span class="nt">-f</span> deploy_rsa deploy_rsa.pub
</code></pre></div></div>

<p>Configure SSH agent in the Travis configuration file:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">before_deploy</span><span class="pi">:</span>
<span class="pi">-</span> <span class="s">openssl aes-256-cbc -K $encrypted_key -iv $encrypted_iv -in .travis/deploy_rsa.enc -out /tmp/deploy_rsa -d</span>
<span class="pi">-</span> <span class="s">eval "$(ssh-agent -s)"</span>
<span class="pi">-</span> <span class="s">chmod 600 /tmp/deploy_rsa</span>
<span class="pi">-</span> <span class="s">ssh-add /tmp/deploy_rsa</span>
</code></pre></div></div>

<p>Then configure the deploy script:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">deploy</span><span class="pi">:</span>
  <span class="na">provider</span><span class="pi">:</span> <span class="s">script</span>
  <span class="na">skip_cleanup</span><span class="pi">:</span> <span class="no">true</span>
  <span class="na">script</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/bin/bash</span><span class="nv"> </span><span class="s">.travis/deploy.sh"</span>
  <span class="na">on</span><span class="pi">:</span>
    <span class="na">branch</span><span class="pi">:</span> <span class="s">master</span>
</code></pre></div></div>

<p>The actual deploy script copies files to the remote directory:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rsync <span class="nt">-r</span> <span class="nt">--delete-after</span> <span class="nt">--quiet</span> <span class="nt">-e</span> <span class="s2">"ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"</span> <span class="nv">$TRAVIS_BUILD_DIR</span>/ <span class="nv">$DEPLOY_USER</span>@<span class="nv">$DEPLOY_HOST</span>:<span class="nv">$DEPLOY_PATH</span>
</code></pre></div></div>

<p>Full configuration can be found <a href="https://github.com/shudarshon/node-js-sample/blob/master/.travis.yml">here</a> and the repository on <a href="https://github.com/shudarshon/node-js-sample/">GitHub</a>.</p>]]></content><author><name>shudarshon chaki ( chaks )</name><email>shudarshon@deriv.com</email><uri>shudarshon.github.io</uri></author><category term="ci/cd" /><category term="travis" /><summary type="html"><![CDATA[Travis CI is another popular CI/CD service which is free for open source projects with limited functionalities. In the free version, unlike Jenkins, Travis CI builds application in a dockerized environment. Travis CI comes with single YAML configuration file where we need specify parameters and configure them to perform successful build and remote deployment.]]></summary></entry></feed>